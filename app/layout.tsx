import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MindMap Pro - Visual Thinking Made Beautiful",
  description: "Create, edit, import and export beautiful mind maps with an intuitive visual interface. Powered by Mind Elixir.",
  keywords: ["mindmap", "mind map", "brainstorming", "visual thinking", "productivity"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
